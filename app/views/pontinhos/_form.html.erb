<%= form_for(@pontinho) do |f| %>
  <% if @pontinho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pontinho.errors.count, "error") %> prohibited this pontinho from being saved:</h2>

      <ul>
      <% @pontinho.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NumDesenho %><br />
    <%= f.number_field :NumDesenho %>
  </div>
  <div class="field">
    <%= f.label :NumPonto %><br />
    <%= f.number_field :NumPonto %>
  </div>
  <div class="field">
    <%= f.label :CX %><br />
    <%= f.number_field :CX %>
  </div>
  <div class="field">
    <%= f.label :CY %><br />
    <%= f.number_field :CY %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
